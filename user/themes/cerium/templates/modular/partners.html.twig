 <section id="partners">
        <div class="heading">
           {{content}}
            <div class="separator"></div>
        </div>
        <div class="container">
            <div class="row">
            {% for partners in page.header.partners %}
                <div class="col-md-3">
                    <div class="partner-logo">
                        <img src="images/partners/{{ partners.img }}.jpg" class="img-responsive" alt="{{partners.alttext}}">
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </section>